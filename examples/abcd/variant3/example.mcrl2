act a,b1,b2,b,c,tick;

proc X = a.b1.X;
proc Z(n: Nat) = (n == 0) -> c.Y
               + (n > 0) -> tick.Z(Int2Nat(n-1));
proc Y = b2.Z(2);

init allow({a,b,c,tick}, 
     	comm({b1|b2->b},
		Y || X
	)
     );

